---
title: "Configure"
description: "all environment variables and configuration options"
weight: 30
---

githome is configured through environment variables. All variables use the
`GITHOME_` prefix.

You can also write them to a file (one `KEY=VALUE` per line) and point githome
at it with `GITHOME_CONFIG_FILE=/path/to/config`.
